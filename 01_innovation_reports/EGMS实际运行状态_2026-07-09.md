# EGMS 实际运行状态
日期：2026-07-09

## 本次执行
已执行：

```powershell
py .\prepare_egms_rescue_queries.py
```

结果：
- 输出目录：`C:\Users\刘子轩\radar_outputs\egms_api_rescue_queries`
- 已生成 EGMS rescue query pack
- 已准备查询：18 个

## 凭据检查
已检查以下位置：
- 环境变量：`EGMS` / `CLMS` / `COPERNICUS`
- Desktop
- Downloads
- Documents
- `科研侦察报告_待上传`
- `radar_outputs`
- `.config`
- `.codex`

结果：
**未发现 `token.jwt`，也未发现 EGMS/CLMS/Copernicus bearer token 环境变量。**

## 官方 API 无 token 验证
请求：

```text
https://egms.land.copernicus.eu/insar-api/archive/levels
```

返回：

```json
{"status": false, "message": "Please log in to perform archive API calls."}
```

HTTP status code 为 200，但 API 业务状态为 false，说明必须登录/携带 token 才能执行 archive API 调用。

## 当前状态
**EGMS 查询包已跑通；EGMS 正式搜索/下载尚未跑通。**

阻塞原因不是脚本或网络，而是本机没有 CLMS API token。

拿到 `token.jwt` 后，直接运行：

```powershell
py .\prepare_egms_rescue_queries.py --token-jwt C:\path\to\token.jwt
```

或已有短期 bearer token：

```powershell
py .\prepare_egms_rescue_queries.py --access-token $env:EGMS_ACCESS_TOKEN
```

预期新增输出：
- `egms_search_results.json`
- `egms_hits.csv`

其中 `egms_hits.csv` 将包含可下载 EGMS 文件列表和下载链接。
