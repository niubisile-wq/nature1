# Final Release Phrase Block v1
Date: 2026-07-13

## Purpose

This block collects the final review-facing phrases that still need external minting or final substitution before submission.

## Replace-in-place text

### Data Availability

The source-data package includes inventories, observability masks, Chao Phraya lead-case outputs, closure tables, transfer-validation scores, figure source-data files, and the release maturity matrix. Reused public datasets are identified in the verified reference list and source-data inventory. The public repository DOI is `[DOI]`, and the version-specific repository URL is `[REPOSITORY_URL]`.

### Code Availability

Custom analysis and figure-generation scripts are included with the source data; the release candidate records the file set and archive hash. The release maturity matrix records which Nature / Scientific Data code and repository expectations are met locally. The versioned repository URL is `[REPOSITORY_URL]`, and the DOI is `[DOI]`.

### Hierarchy limitation

The all-region hierarchical artifact is a closure-by-compilation appendix, not a fresh all-region fit. It is included to make the current evidence auditable, not to claim a new validation split.

### Reviewer appendix entry point

Use `cross_paper_reviewer_appendix_index_v1` as the single entry point for the five-paper comparison package.

## Bottom line

This block is the last substitution layer before minting. The only external steps still outstanding are DOI / repository minting and the final text replacement.
